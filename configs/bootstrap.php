<?php

// Bootstrap file for PHPStan analysis of FluentCRM stubs.
// Provides minimal WordPress stubs when wp-stubs aren't loaded.

if ( ! defined( 'ABSPATH' ) ) {
    define( 'ABSPATH', __DIR__ . '/' );
}
